#include<stdio.h>
/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
	int m;
	for (m = 48; m < 59; m++)
	{
		putchar(m);
		if (n != 58)
		{
			putchar(',');
			putchar('');
		}	
	}
	putchar('\n');
	return (0);
}
